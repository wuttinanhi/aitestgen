{
  "type": "function",
  "function": {
    "name": "clickElement",
    "description": "Clicks on an element on the page using a CSS selector",
    "parameters": {
      "type": "object",
      "properties": {
        "selector": {
          "type": "string",
          "description": "The CSS selector of the element to click"
        }
      },
      "required": [
        "selector"
      ],
      "additionalProperties": false
    }
  }
}