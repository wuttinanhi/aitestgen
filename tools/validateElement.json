{
  "type": "function",
  "function": {
    "name": "validateElement",
    "description": "Validates if an element exists on the page using a CSS selector",
    "parameters": {
      "type": "object",
      "properties": {
        "selector": {
          "type": "string",
          "description": "The CSS selector of the element to validate"
        }
      },
      "required": [
        "selector"
      ],
      "additionalProperties": false
    }
  }
}