{
  "type": "function",
  "function": {
    "name": "expectElementVisible",
    "description": "Checks if an element is visible or hidden on the page",
    "parameters": {
      "type": "object",
      "properties": {
        "selector": {
          "type": "string",
          "description": "The CSS selector of the element to check visibility"
        },
        "visible": {
          "type": "boolean",
          "description": "Whether the element should be visible (true) or hidden (false)"
        }
      },
      "required": [
        "selector",
        "visible"
      ],
      "additionalProperties": false
    }
  }
}